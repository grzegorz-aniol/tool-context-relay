# Codex execution policy rules (team-config, checked into the repo).
# These rules apply when Codex needs to run commands outside the sandbox.
#
# Docs: https://developers.openai.com/codex/exec-policy

# Allow running CI locally without interactive approval prompts.
prefix_rule(pattern = ["make", "ci"], decision = "allow")

# Allow docker compose workflows without interactive approval prompts.
prefix_rule(pattern = ["docker", "compose"], decision = "allow")
